# Same as the root .env.example — use that for local development.
# For Vercel, set this in Project → Settings → Environment Variables.

PRISMA_DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
